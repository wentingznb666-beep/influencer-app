# 生产环境务必设置（Render 等平台在 Environment 中配置）
JWT_SECRET=your-random-secret-at-least-32-chars
JWT_REFRESH_SECRET=another-random-refresh-secret

# 可选：数据库路径（默认 ./data/app.db，Render 免费版重启后可能清空）
# DB_PATH=/data/app.db

# 可选：端口（Render 自动注入 PORT）
# PORT=3000

# 若使用翻译/朗读功能再配置
# DEEPSEEK_API_KEY=
# AZURE_SPEECH_KEY=
# AZURE_SPEECH_REGION=
