# ngrok 隧道配置：同时暴露后端(3000)和前端(5173)，用于生成外网可访问的网址
# 使用方式：先启动后端与前端，再在本目录执行 ngrok start --config ngrok.yml backend frontend
version: "2"
tunnels:
  backend:
    addr: 3000
    proto: http
  frontend:
    addr: 5173
    proto: http
